;;; -*- lexical-binding : t ; byte-compile-dynamic : t -*-

;;; Commentary:

;;; Code:

(eval-when-compile (/require-meta file))

(/def-config-directory company
  "The directory to save company configuration.
For example, the cache file `company-statistics-cache.el'")

(/provide)
;;; custom/company.el ends here
