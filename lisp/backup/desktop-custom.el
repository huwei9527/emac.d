;; -*- lexical-binding : t ; byte-compile-dynamic : t -*-

(eval-when-compile
  (require 'file-code))

(code-defdir-config "desktop"
  "The directory to store desktop session files.")

(provide 'desktop-custom)
;;; desktop-custom.el ends here
