;; -*- lexical-binding : t ; byte-compile-dynamic : t -*-

(defun set-mode-line-for-evil ()
  "Set the modeline by current evil-state."
  (cond
   ;((evil-insert-state-p) set-e)
   ))

(provide 'evil-lib)
; evil-lib.el ends here